name: testing

on:
    push:

jobs:
    testing:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: ./.github/actions/get-latest-tag
              id: get-latest-sha
            - run: echo ${{ steps.get-latest-sha.outputs.tag-name }}
            - run: echo "All done!"
